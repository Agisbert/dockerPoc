spring:
 profiles: default
 application:
  name: registry-server
 cloud:
  config:
   enabled: false
   
---
spring:
 profiles: config-server
 application:
  name: registry-server
 cloud:
  config:
   enabled: true
   uri: http://config-container:8090
   fail-fast: true